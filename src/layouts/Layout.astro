---
import "../styles/global.css";
---

<!doctype html>
<html lang="fr">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="generator" content={Astro.generator} />
    <title>Analyse des Manchots - TP2</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
  </head>
  <body class="font-inter bg-gradient-to-br from-slate-50 to-blue-50 min-h-screen">
    
    <!-- Navigation principale moderne -->
    <nav class="sticky top-0 z-40 backdrop-blur-lg bg-white/80 border-b border-white/20 shadow-lg">
      <div class="container mx-auto px-6 py-4">
        <div class="flex items-center justify-between">
          
          <!-- Logo/Titre -->
          <div class="flex items-center gap-3">
            <div class="w-10 h-10 bg-gradient-to-br from-blue-500 to-purple-600 rounded-lg flex items-center justify-center">
              <span class="text-white font-bold text-lg">🐧</span>
            </div>
            <div>
              <h1 class="text-xl font-bold bg-gradient-to-r from-blue-600 to-purple-600 bg-clip-text text-transparent">
                Analyse Manchots
              </h1>
              <p class="text-xs text-gray-500">TP2 - Visualisation de données</p>
            </div>
          </div>

          <!-- Menu principal -->
          <div class="flex items-center gap-2">
            
            <!-- Groupe Espèces -->
            <div class="flex items-center bg-white/60 rounded-full p-1 shadow-sm border border-white/30">
              <span class="text-sm font-medium text-gray-600 px-3">Espèces:</span>
              <a href="/species/Adelie" 
                 class="nav-link px-4 py-2 rounded-full text-sm font-medium transition-all duration-300 hover:bg-orange-500 hover:text-white hover:shadow-md hover:scale-105"
                 target="_top">
                🐧 Adelie
              </a>
              <a href="/species/Gentoo" 
                 class="nav-link px-4 py-2 rounded-full text-sm font-medium transition-all duration-300 hover:bg-teal-500 hover:text-white hover:shadow-md hover:scale-105"
                 target="_top">
                🐧 Gentoo
              </a>
              <a href="/species/Chinstrap" 
                 class="nav-link px-4 py-2 rounded-full text-sm font-medium transition-all duration-300 hover:bg-purple-500 hover:text-white hover:shadow-md hover:scale-105"
                 target="_top">
                🐧 Chinstrap
              </a>
            </div>

            <!-- Séparateur -->
            <div class="w-px h-6 bg-gray-300"></div>

            <!-- Groupe Vues -->
            <div class="flex items-center gap-2">
              <a href="/test-iframe" 
                 class="nav-special-link flex items-center gap-2 px-4 py-2 bg-gradient-to-r from-indigo-500 to-purple-600 text-white rounded-full text-sm font-medium transition-all duration-300 hover:shadow-lg hover:scale-105"
                 target="_top">
                <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 17V7m0 10a2 2 0 01-2 2H5a2 2 0 01-2-2V7a2 2 0 012-2h2a2 2 0 012 2m0 10a2 2 0 002 2h2a2 2 0 002-2M9 7a2 2 0 012-2h2a2 2 0 012 2"></path>
                </svg>
                Iframe
              </a>
              
              <a href="/all-species" 
                 class="nav-special-link flex items-center gap-2 px-4 py-2 bg-gradient-to-r from-emerald-500 to-teal-600 text-white rounded-full text-sm font-medium transition-all duration-300 hover:shadow-lg hover:scale-105"
                 target="_top">
                <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z"></path>
                </svg>
                Ancres
              </a>
            </div>
          </div>
        </div>
      </div>
    </nav>

    <!-- Contenu principal -->
    <main class="flex-1">
      <slot />
    </main>

    <!-- Footer moderne -->
    <footer class="mt-auto bg-white/60 backdrop-blur-sm border-t border-white/20">
      <div class="container mx-auto px-6 py-6">
        <div class="flex items-center justify-between text-sm text-gray-600">
          <div class="flex items-center gap-4">
            <span>© 2025 TP2 Manchots</span>
            <span class="text-gray-400">•</span>
            <span>Astro + Observable Plot</span>
          </div>
          <div class="flex items-center gap-4">
            <span class="px-3 py-1 bg-green-100 text-green-700 rounded-full text-xs font-medium">
              ✓ Déployé sur Netlify
            </span>
          </div>
        </div>
      </div>
    </footer>

  </body>
</html>

<style>
  .font-inter {
    font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
  }

  /* Animations pour les liens de navigation */
  .nav-link {
    position: relative;
    overflow: hidden;
  }

  .nav-link:hover {
    transform: translateY(-1px);
  }

  .nav-link::before {
    content: '';
    position: absolute;
    top: 0;
    left: -100%;
    width: 100%;
    height: 100%;
    background: linear-gradient(90deg, transparent, rgba(255,255,255,0.3), transparent);
    transition: left 0.5s;
  }

  .nav-link:hover::before {
    left: 100%;
  }

  .nav-special-link {
    position: relative;
    overflow: hidden;
  }

  .nav-special-link:hover {
    transform: translateY(-1px) scale(1.02);
  }

  /* Animation de page load */
  @keyframes slideInFromTop {
    from {
      opacity: 0;
      transform: translateY(-20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  nav {
    animation: slideInFromTop 0.6s ease-out;
  }

  main {
    animation: slideInFromTop 0.8s ease-out 0.2s both;
  }

  /* Smooth transitions globales */
  * {
    transition-property: color, background-color, border-color, text-decoration-color, fill, stroke, opacity, box-shadow, transform, filter, backdrop-filter;
    transition-timing-function: cubic-bezier(0.4, 0, 0.2, 1);
    transition-duration: 150ms;
  }

  /* Scrollbar personnalisée */
  ::-webkit-scrollbar {
    width: 6px;
  }

  ::-webkit-scrollbar-track {
    background: rgba(0,0,0,0.1);
    border-radius: 3px;
  }

  ::-webkit-scrollbar-thumb {
    background: linear-gradient(to bottom, #60a5fa, #3b82f6);
    border-radius: 3px;
  }

  ::-webkit-scrollbar-thumb:hover {
    background: linear-gradient(to bottom, #3b82f6, #1d4ed8);
  }

  /* Responsive */
  @media (max-width: 768px) {
    nav .container {
      flex-direction: column;
      gap: 4;
    }
    
    .nav-link, .nav-special-link {
      font-size: 0.75rem;
      padding: 0.375rem 0.75rem;
    }
  }
</style>